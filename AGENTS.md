# AGENTS.md

本文件记录本仓库的代理协作入口信息。全局执行规则仍以 `C:\Users\kpy\.codex\AGENTS.md` 和 `C:\Users\kpy\.codex\docs\development-guidelines.md` 为准；本文件只补充当前仓库的技能配置。

## Agent skills

### Issue tracker

Issue 和 PRD 使用 GitHub Issues 管理。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用 mattpocock/skills 默认五类 triage 标签。详见 `docs/agents/triage-labels.md`。

### Domain docs

本仓库是 single-context 项目：读取根目录 `CONTEXT.md` 和相关 `docs/adr/`。详见 `docs/agents/domain.md`。
